@model List<GimnasioCuerpoSano.Models.Miembro>

@{
    Layout = null; // que no use layout
    ViewData["Title"] = "Listado de Miembros";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <!-- Incluimos Bootstrap para las clases de tabla y botones -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/site.css" asp-append-version="true" />

    <style>
        body {
            background-color: #f8f9fa;
            padding: 2rem;
            font-family: 'Segoe UI', sans-serif;
        }

        /* NUEVA CLASE PARA CENTRAR EL LISTADO */
        .list-container {
            max-width: 1400px; /* Ancho máximo para el contenido, puedes ajustarlo si quieres más o menos */
            margin: 0 auto; /* Centra el contenedor horizontalmente */
            padding: 0 15px; /* Pequeño padding lateral para responsividad */
        }

        .card {
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
            border-radius: 10px;
        }

        table {
            background: white;
            border-radius: 8px;
            overflow: hidden;
        }

        th {
            background-color: #0d6efd;
            color: white;
        }

        .btn {
            border-radius: 8px;
        }

        h2 {
            color: #0d6efd;
            margin-bottom: 1.5rem;
        }
    </style>
</head>
<body>

    <!-- APLICAMOS LA CLASE list-container AL DIV PRINCIPAL -->
    <div class="list-container">
        <div class="text-center mb-4">
            <h2>Listado de Miembros</h2>

            @if (TempData["Mensaje"] != null)
            {
                <div class="alert alert-success">@TempData["Mensaje"]</div>
            }

            <a asp-action="Create" class="btn btn-success mb-3">Agregar nuevo miembro</a>
        </div>

        @if (Model != null && Model.Any())
        {
            <!-- La tabla está envuelta en table-responsive y usa las clases de Bootstrap -->
            <div class="table-responsive card">
                <table class="table table-striped table-bordered align-middle text-center">
                    <thead>
                        <tr>
                            <th>Tipo Documento</th>
                            <th>DNI</th>
                            <th>Nombre</th>
                            <th>Apellido</th>
                            <th>Dirección</th>
                            <th>Teléfono</th>
                            <th>Mail</th>
                            <th>Tipo</th>
                            <th>Descuento</th>
                            <th>Valor</th>
                            <th>Fecha Alta</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (int i = 0; i < Model.Count; i++)
                        {
                            var item = Model[i];
                            <tr>
                                <td>@item.TipoDocumento</td>
                                <td>@item.DNI</td>
                                <td>@item.Nombre</td>
                                <td>@item.Apellido</td>
                                <td>@item.Direccion</td>
                                <td>@item.Telefono</td>
                                <td>@item.Mail</td>
                                <td>@item.Membresia?.Nombre</td>
                                <td>@(item.DescuentoEspecial ? "Sí" : "No")</td>
                                <td>$@item.ValorMembresia</td>
                                <td>@item.FechaAlta.ToString("dd/MM/yyyy HH:mm")</td>
                                <td>
                                    <!-- Agrupamos los botones en un div para mejor control de espacio -->
                                    <div class="d-grid gap-2 d-md-block">
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Editar</a>
                                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm">Ver</a>
                                        <!-- Usamos un botón para Delete que puede requerir confirmación -->
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Eliminar</a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="alert alert-info text-center">No hay miembros registrados.</div>
        }

        <!-- También centramos los botones inferiores en el listado -->
        <div class="text-center mt-4">
            <a asp-action="Create" class="btn btn-success px-4">Nueva Membresía</a>
            <a asp-controller="Home" asp-action="Index" class="btn btn-secondary px-4 ms-2">Volver al Menú</a>
        </div>
    </div> <!-- Cierre de .list-container -->

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

